services:
  - type: web
    name: personalized-news-aggregator
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3004
    plan: free
